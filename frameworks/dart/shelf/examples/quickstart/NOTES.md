# Shelf 设计笔记

- 版本基线：latest stable
- 语言基线：Dart 3.12.x
- 核心案例：内存 tasks API
- 当前状态：已提供 `pubspec.yaml`、`bin/server.dart`、`test/server_test.dart` 和可复制运行命令。
- 后续扩展：加入 `shelf_router`、输入校验、数据库 repository、鉴权和容器部署。

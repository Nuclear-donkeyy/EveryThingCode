# NestJS 设计笔记

- 版本基线：latest stable
- 语言基线：JavaScript / TypeScript Node.js 24.16.0 LTS
- 核心案例：模块化 API
- 已提供：依赖声明、TypeScript 配置、根模块、业务模块、控制器、Provider、管道、守卫和 `npm run smoke`。
- 真实运行：在本目录执行 `npm install` 后使用 `npm run start:dev`。

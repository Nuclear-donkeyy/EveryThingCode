# JavaScript / TypeScript / errors

## 目标

通过一个最小案例观察 JavaScript / TypeScript 在 `errors` 场景下的惯用写法。

## 运行

```bash
node main.mjs
```

## 预期输出

输出应包含 `Hello`、`total minutes` 或 `recover` 之一，分别对应最小程序、数据流和错误恢复案例。

## 观察点

- 源文件：`main.mjs`
- 版本基线：Node.js 24.16.0 LTS
- 包管理：npm / pnpm / yarn

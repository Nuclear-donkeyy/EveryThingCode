# PHP / data-flow

## 目标

通过一个最小案例观察 PHP 在 `data-flow` 场景下的惯用写法。

## 运行

```bash
php main.php
```

## 预期输出

输出应包含 `Hello`、`total minutes` 或 `recover` 之一，分别对应最小程序、数据流和错误恢复案例。

## 观察点

- 源文件：`main.php`
- 版本基线：8.5.x
- 包管理：Composer
